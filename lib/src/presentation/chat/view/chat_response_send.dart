import 'package:flutter/material.dart';

class ChatResponseSend extends StatelessWidget {
  const ChatResponseSend({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("ChatResponseSend");
  }
}
