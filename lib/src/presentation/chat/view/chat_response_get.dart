import 'package:flutter/material.dart';

class ChatResponseGet extends StatelessWidget {
  const ChatResponseGet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("ChatResponseGet");

  }
}
