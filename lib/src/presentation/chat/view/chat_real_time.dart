import 'package:flutter/material.dart';

class ChatRealTime extends StatelessWidget {
  const ChatRealTime({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("ChatRealTime");
  }
}
